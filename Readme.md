# i.maybea.dev

A gateway that redirects a request for `https://i.maybea.dev/*` to `https://*.mudit.xyz/*`.

AKA the largest [zeit/now](https://zeit.co/now) project.

Take a look at `now.json` to see the redirects.
